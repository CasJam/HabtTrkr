{
  "name": "app",
  "private": true,
  "devDependencies": {
    "esbuild": "^0.25.5"
  },
  "scripts": {
    "build": "esbuild app/javascript/*.* --bundle --sourcemap --format=esm --outdir=app/assets/builds --public-path=/assets --loader:.js=jsx --jsx=transform"
  },
  "dependencies": {
    "@inertiajs/react": "^2.0.11",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  }
}
